package practice.javaprograms.forloop;

import java.util.Scanner;

public class Lab005_TableOf5UsingLoop {
    int x = 5;
    public static void main(String[] args) {

        Lab005_TableOf5UsingLoop myObj = new Lab005_TableOf5UsingLoop();
                System.out.println(myObj.x);
            }
        }
        /*Scanner sc = new Scanner(System.in);
        System.out.println("Enter numbers for which table needs to be printed and number till the table needs to be printed");
        while (true) {
            if (sc.hasNextLine()) {
                String[] input = sc.nextLine().split("\\s+");
                for (String value : input) {
                    int number = Integer.parseInt(value);
                    System.out.println(number);
                    if (number <= 0)
                        System.out.println("Enter positive numbers");
                }

            } else*/
               // System.out.println("Input valid values");
        /*if(!(sc.hasNextInt()) || !(sc.hasNextInt()))
            System.out.println("Enter an integer number");
        else
        {
            int number = sc.nextInt();
            System.out.println("Enter a ");
            int limit = sc.nextInt();
            if(number <=0  || (limit <=0 || limit > Integer.MAX_VALUE))
                System.out.println("Enter positive number within range");
            else
            {
                for (int i = 1; i <=limit; i++) {
                    System.out.printf("%d*%d = %d \n", number,i, number*i);

                }
            }*/
     


